spring:
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        format_sql: 'true'
        generate_statistics: 'true'
  sql:
    init:
      mode: always

logging:
  level:
    org.hibernate.stat: DEBUG